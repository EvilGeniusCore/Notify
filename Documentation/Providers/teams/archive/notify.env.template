# notify.env — Graph API credentials (RSC approach — archived March 2026)
# This credential model is no longer used. Retained for reference only.
# See archive/notify-teams-channelmessage-blocker.md for context.
#
# Usage was: notify send --env-file ./notify.env --team "..." --channel "..." --message "..."

NOTIFY_TEAMS_TENANT_ID=
NOTIFY_TEAMS_CLIENT_ID=
NOTIFY_TEAMS_CLIENT_SECRET=
NOTIFY_TEAMS_DEFAULT_TEAM=
NOTIFY_TEAMS_DEFAULT_CHANNEL=
